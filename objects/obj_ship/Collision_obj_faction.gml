
// check if object is member of same faction
if (other.faction == faction) exit;

// performs take damage event if object is not from the same faction
event_perform(ev_other, ev_user0)

// reduces ships HP by 1
hp -= 1;



